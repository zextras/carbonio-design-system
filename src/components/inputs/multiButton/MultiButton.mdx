import { Meta, Controls, Stories, Title, Primary } from '@storybook/blocks';
import * as MultiButtonStories from './MultiButton.stories';

<Meta of={MultiButtonStories} />

<Title />

MultiButton is basically a Button with a Dropdown linked to the secondary action.
It can have a label or an icon as primary element.
Secondary icon can be customized.

<Primary />

<Controls />

<Stories />
