import { Meta, Controls, Stories, Title, Primary } from '@storybook/blocks';
import * as QuotaStories from './Quota.stories';

<Meta of={QuotaStories} />

<Title />

The quota element is used to display the user's quota usage percentage.

It can also accept all props of the Container component.

<Primary />

<Controls />

<Stories />
